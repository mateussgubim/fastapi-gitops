apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastzero.fullname" . }}
  labels:
    {{- include "fastzero.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastzero.selectorLabels" . | nindent 4 }}
